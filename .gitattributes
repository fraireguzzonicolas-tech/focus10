# NADA DE TOCAR LOS FINALES DE LINEA.
#
# Esta carpeta es un COMPILADO: lo que se sube tiene que llegar al navegador
# byte a byte igual que lo genero flet. Git, por su cuenta, cambia los finales
# de linea de todo lo que le parece texto —aqui eran 34 archivos, entre ellos
# el service worker y el arranque de Flutter— y eso son bytes distintos a los
# que se probaron.
#
# El "-text" apaga esa conversion para todo.
* -text
